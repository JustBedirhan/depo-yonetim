services:
  - type: web
    name: depo-yonetim
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: depo-db
          property: connectionString

databases:
  - name: depo-db
    plan: free
